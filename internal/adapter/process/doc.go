// Package process implements direct operating-system command execution with
// bounded output, streaming, typed failures, and cancellation. Consumers define
// the narrow runner interfaces they require; OSRunner satisfies them implicitly.
package process
